import {StyleSheet} from 'react-native';

const gradientButtonStyle = StyleSheet.create({
  button: {
    backgroundColor: 'transparent',
    padding: 20,
    borderRadius: 48,
    justifyContent: 'center',
    alignItems: 'center',
  },
  textButton: {
    color: 'white',
    fontSize: 16,
    fontWeight: '600',
  },
});

export default gradientButtonStyle;
